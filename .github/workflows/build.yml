name: Build ESP32 Custom 4MB App

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    # 🚀 NEW STEP: Unzip your audio library so the compiler can read it
    - name: Unzip Audio Library
      run: unzip ESP32-audioI2S-master.zip -d ./libraries

    - name: Setup Arduino CLI
      uses: arduino/setup-arduino-cli@v1

    - name: Install ESP32 Core
      run: |
        arduino-cli core update-index
        arduino-cli core install esp32:esp32

    - name: Install Project Dependency Libraries
      run: |
        arduino-cli lib install "Adafruit MPU6050"
        arduino-cli lib install "Adafruit Unified Sensor"
        arduino-cli lib install "Adafruit BusIO"
        arduino-cli lib install "Adafruit SSD1306"
        arduino-cli lib install "Adafruit GFX Library"
        arduino-cli lib install "ArduinoJson"

    - name: Compile with Custom 3MB Memory Layout
      run: |
        arduino-cli compile \
        --fqbn esp32:esp32:XIAO_ESP32S3 \
        --build-property "build.custom_partitions=${{ github.workspace }}/partitions.csv" \
        --libraries ./libraries/ESP32-audioI2S \
        --output-dir build \
        --export-binaries \
        main

    - name: Upload Compiled Firmware Binary
      uses: actions/upload-artifact@v4
      with:
        name: firmware
        path: build/*.bin
